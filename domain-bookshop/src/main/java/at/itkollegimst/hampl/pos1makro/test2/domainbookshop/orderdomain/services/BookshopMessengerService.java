package at.itkollegimst.hampl.pos1makro.test2.domainbookshop.orderdomain.services;

//Should connect all the microservices over RabbitMQ
import com.rabbitmq.client.Channel;
import org.springframework.messaging.MessageChannel;
import org.springframework.messaging.handler.annotation.MessageMapping;

public class BookshopMessengerService {
    /*
    private static final String BOOKORDER_CREATED_CHANNEL = "bookOrderCreatedChannel";

    @Input(BOOKORDER_CREATED_CHANNEL)
    MessageChannel bookOrderCreatedChannel();
    */
}
